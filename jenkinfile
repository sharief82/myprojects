pipeline {
    agent any

    stages {
        stage ('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello from junkin"'
                sh 'whoami'
                sh 'df -h'
                sh 'hostname -I'
            }
        }
    }
}